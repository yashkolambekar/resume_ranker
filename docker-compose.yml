version: '3.8'

services:
  resume-ranker:
    build: .
    container_name: resume-ranker
    ports:
      - "3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
